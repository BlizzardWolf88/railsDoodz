
<br/>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

 
 <div class= "container d-flex justify-content-center">
  <div class="card text-center" style="width: 30rem;" > 
    <div class="card-header">
      <h2>FORGOT YOUR PASSY MADDOO????</h2> 
    </div>
    <div class="card-body"> 
     <div class="field">
       <h5><%= f.label :email,class: "form-label"  %></h5>
        <%= f.email_field :email, autofocus: true,class:"form-control",placeholder:"Your Email Madoo" ,autocomplete: "email" %>
       </div>

          <br/>
          <div class="actions">
            <%= f.submit "Send me reset password instructions" , class:"btn btn-primary btn-lg active" %>
          </div>       
    </div>
      <div class="card-footer text-muted"><strong>Become A Dood Madoo</strong></div>
    </div>

  

  <% end %>
</div>

<br/>
<%= render "devise/shared/links" %>
